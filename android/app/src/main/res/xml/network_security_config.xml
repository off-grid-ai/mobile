<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow cleartext (HTTP) only to localhost and emulator host addresses for Metro bundler + LocalDream -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">10.0.3.2</domain>
    </domain-config>
    <!--
         Android network_security_config.xml has no IP-range wildcard support, so we cannot
         restrict cleartext to private RFC-1918 ranges only. The base-config must allow HTTP
         so users can connect to LAN LLM servers (Ollama, LM Studio, LocalAI) on arbitrary
         private IPs. All outbound connections to the public internet remain HTTPS-only
         because those servers redirect HTTP → HTTPS.
    -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
